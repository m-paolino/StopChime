#!bin/bash

logvolume = "/Library/StopChime/stopchime_logvolume"

# reading the value stored in stopchime_logvolume
read value < "$logvolume"

# restoring the volume state
if $value>0	then
	osascript -e 'if set volume output volume '$value''
